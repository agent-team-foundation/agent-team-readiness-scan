# Monorepo instructions

## Required reading

Read the scoped AGENTS.md before editing a package.

```bash
pnpm test
pnpm lint
pnpm build
```

Never edit generated files. Keep each task on a separate branch or git worktree.
